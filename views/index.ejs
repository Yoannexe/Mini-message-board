<html>
    <head>
        <title><%= title%></title>
    </head>
    <body>
        <% for (let i = 0 ; i < messages.length ; i++) { %>
            <span>Message : <%= messages[i].text %></span><br>
            <span>User : <%= messages[i].user %></span><br>
            <span>Date : <%= messages[i].added %></span><br>
        <% }%>
    </body>
</html>
